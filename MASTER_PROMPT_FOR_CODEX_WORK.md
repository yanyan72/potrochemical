# 发给 Codex / Work 的总启动 Prompt

> 历史启动材料。2026-09-18用户已收缩到运输仓储；生产、物理约束和区块链为未来扩展。下面的旧Milestone 0任务、全生命周期范围和模型参数不是当前执行指令。请以AGENTS.md、docs/PROJECT_BRIEF.md、docs/STATUS.md和当前ExecPlan为准，不从零重启或恢复旧范围。

你现在是本项目的“科研工程负责人 + 算法实现负责人 + 实验审稿人”。请读取本仓库中的全部项目说明文件，尤其是：

- `AGENTS.md`
- `README.md`
- `docs/PROJECT_BRIEF.md`
- `docs/ARCHITECTURE.md`
- `docs/DATA_SPEC.md`
- `docs/EXPERIMENT_PLAN.md`
- `docs/TASKS.md`
- `docs/DECISIONS.md`
- `.agents/PLANS.md`
- `.agents/skills/petrochemical-trust-research/SKILL.md`
- `docs/source_materials/` 中的原始项目材料

## 一、项目名称

**区块链 + AI 双驱动的石化产品全生命周期溯源与质量智能预警研究**

## 二、项目定位

这是一个本科生科研项目，但必须按照可复现的科研工程标准推进。项目创新重点必须落在：

1. 多维传感器数据的可信度建模；
2. 可信集与低可信集划分；
3. 可信度加权或可信度修正的时序质量预测；
4. 物理动力学约束下的风险累积预测；
5. 区块链完整性信息与 AI 可信度的融合；
6. 完整的对照实验、消融实验、鲁棒性实验和复现流程。

**禁止把“使用了区块链、LSTM、PINN、Transformer”本身当作创新。**  
**禁止先做平台界面、智能合约演示或宏大场景包装。**  
第一阶段必须用算法和实验回答：可信度机制是否有效、物理约束是否有效、两者结合是否进一步有效。

## 三、当前研究阶段

前序论文阅读和方案构思已经基本完成，当前进入“动手实现”阶段。现有方案仍包含概念性内容，需要先完成最小算法闭环，再逐步增加模块。

当前优先顺序：

1. 仿真数据与异常注入；
2. 多维可信度识别；
3. 普通 LSTM 基线；
4. 可信度修正/加权 LSTM；
5. 动力学约束 LSTM；
6. 可信度 + 动力学约束完整模型；
7. 区块链哈希完整性验证；
8. 全部实验、图表、文档与阶段汇报。

## 四、已经确认的概念修正

请在所有代码、文档和汇报中遵守：

1. `dQ/dt` 是质量变化速率，不是“质量下降加速度”；二阶导数才对应变化加速度。
2. 第一版离散 LSTM 不直接宣称对时间做连续自动微分；先使用有限差分或离散动力学递推。
3. 区块链只能证明数据写入后的完整性和可追溯性，不能证明传感器上链前采集的数据天然真实。
4. 网络若预测动态 `A_t`、`E_a,t`，必须有取值范围、正值约束和平滑约束，否则物理约束可能被模型任意参数化而失效。
5. 第一阶段聚焦“储运或连续工艺时序中的质量劣化预测”，这只是算法验证载体；不要一开始试图覆盖整个全生命周期。
6. PCA 主要用于二维可视化；可信度计算原则上基于标准化后的原始有效特征空间，除非实验明确比较降维后效果。
7. 不得使用测试集计算标准化参数、阈值或协方差矩阵；所有这些参数只能从训练集或正常训练子集估计。
8. 不得伪造真实石化数据、实验结果、论文引用或模型性能。

## 五、第一阶段正式研究问题

每个时刻的多源传感器数据表示为一个向量：

\[
x_t=[T_t,P_t,V_t,C_t]
\]

其中可依次表示温度、压力、振动、浓度或其他工艺变量。全部样本构成矩阵：

\[
X\in\mathbb{R}^{N\times d}
\]

目标是：

1. 根据正常训练数据构建参考可信集合；
2. 计算每个数据点相对可信集合的偏离程度；
3. 将偏离程度转换为连续可信度 `w_t ∈ [0,1]`；
4. 识别高可信集、不确定集和低可信集；
5. 利用可信度对异常数据进行修正或加权；
6. 比较普通 LSTM 与可信度增强 LSTM 的质量预测性能；
7. 验证污染比例增加时，可信度增强模型是否具有更强鲁棒性。

## 六、第一版可信度算法

第一版使用可解释的统计方法，不要一开始堆叠多种异常检测算法。

### 1. 标准化

仅使用正常训练集计算每列的均值和标准差：

\[
x'_{tj}=\frac{x_{tj}-\mu_j}{\sigma_j}
\]

### 2. 可信集中心与协方差

从正常训练集估计：

\[
\mu=\frac{1}{N}\sum_t x_t
\]

\[
\Sigma=\frac{1}{N-1}\sum_t(x_t-\mu)(x_t-\mu)^T
\]

协方差可能病态，优先使用收缩协方差估计、正则化逆矩阵或伪逆，并记录选择原因。

### 3. 马氏距离

\[
d_t^2=(x_t-\mu)^T\Sigma^{-1}(x_t-\mu)
\]

使用正常训练集距离分布的分位数划分：

- `d² <= q90`：高可信集；
- `q90 < d² <= q99`：不确定集；
- `d² > q99`：低可信集。

分位数先作为基线超参数，不得宣称是最终最优阈值。

### 4. 连续可信度

实现一个单调递减、范围在 `[0,1]` 的可信度映射，并保证数值稳定。例如：

\[
w_t=\exp\left(-\frac{d_t^2}{2\tau}\right)
\]

其中 `τ` 由正常训练距离分布确定，或采用分段映射。请把映射写成可配置函数并添加单元测试。

### 5. 可信度修正

对异常或低可信点，基于局部高可信邻域计算稳健参考值：

\[
x_t^{ref}=\operatorname{median}(x_{t-k:t+k}^{high})
\]

再融合：

\[
\tilde{x}_t=w_tx_t+(1-w_t)x_t^{ref}
\]

需要正确处理窗口内无高可信点、边界时刻和缺失值。

## 七、仿真数据要求

第一版只做数值型时序数据。必须包含：

- 温度 `T`
- 压力 `P`
- 振动 `V`
- 浓度/工艺变量 `C`
- 质量指标 `Q ∈ [0,1]`
- 工况标签
- 异常标签
- 异常类型
- 序列 ID 和时间索引

至少实现以下异常：

1. spike：瞬时脉冲；
2. bias：持续偏移；
3. drift：缓慢漂移；
4. missing：缺失；
5. random replacement：随机替换；
6. timestamp misalignment：时间错位（第二轮）。

每次异常注入必须返回异常掩码，不允许丢失真值标签。

仿真质量变化必须具有可控规律。第一版可用离散递推，第二版再引入阿伦尼乌斯：

\[
Q_{t+1}=\mathrm{clip}(Q_t-r_t\Delta t+\epsilon_t,0,1)
\]

仿真器要支持：

- 固定随机种子；
- 参数配置文件；
- 训练/验证/测试按完整序列划分；
- 干净真值与污染观测同时保存；
- 不同污染比例；
- 数据生成日志和元数据。

## 八、时序预测基线

第一版模型：

- 单层 LSTM；
- 输入窗口 `L=24`；
- 先做单步预测 `Q_{t+1}`；
- 隐藏维度初始值 32；
- Adam；
- MSE 或 Huber；
- 配置文件控制参数；
- 至少与持久性预测和线性回归比较。

对比模型：

- `M0`：持久性预测；
- `M1`：线性回归；
- `M2`：原始污染数据 LSTM；
- `M3`：中位数修正 LSTM；
- `M4`：可信度修正 LSTM；
- `M5`：可信度作为附加输入的 LSTM；
- 后续 `M6`：动力学约束 LSTM；
- 后续 `M7`：可信度 + 动力学完整模型。

第一轮不要同时修改输入、损失、网络结构，否则无法归因。每次只增加一个因素。

## 九、物理动力学阶段

仅在可信度实验跑通后进入。

阿伦尼乌斯基线：

\[
r_t=A\exp\left(-\frac{E_a}{RT_t}\right)
\]

离散质量递推：

\[
Q_{t+1}^{phy}=Q_t\exp(-r_t\Delta t)
\]

物理一致性损失：

\[
L_{physics}=\sum_t \operatorname{Huber}(\hat Q_{t+1},\hat Q_t\exp(-r_t\Delta t))
\]

总损失：

\[
L=\lambda_dL_{data}+\lambda_pL_{physics}+\lambda_fL_{forecast}+\lambda_sL_{smooth}
\]

若动态预测 `A_t` 和 `E_a,t`：

- `A_t > 0`；
- `E_min <= E_a,t <= E_max`；
- 添加时间平滑正则；
- 必须做“固定参数 vs 动态参数”消融；
- 必须检查参数可辨识性和物理合理性；
- 不得仅因训练误差更低就声称物理解释成立。

## 十、风险累积与预警

建议使用累积风险或累积危险率形式：

\[
R_t=1-\exp\left(-\sum_{i=1}^{t}r_i\Delta t\right)
\]

或：

\[
R_t=1-\frac{Q_t}{Q_0}
\]

必须明确采用哪一种、其物理含义及适用边界。

预警任务包括：

- 质量是否会越过阈值；
- 越过阈值的预计时间；
- 平均提前预警时间；
- 误报率和漏报率；
- 不同风险等级。

## 十一、区块链阶段

第一版只实现轻量哈希链，不部署复杂平台：

\[
H_i=SHA256(H_{i-1}\Vert timestamp_i\Vert data_i)
\]

区分：

1. 上链后篡改：哈希链应检测；
2. 上链前错误：哈希链无法检测，需由传感器可信度或物理一致性发现。

综合可信度可研究：

\[
w_t^{final}=w_t^{space}\cdot w_t^{physics}\cdot w_t^{chain}
\]

但必须比较乘法、加权和或可学习融合，不应默认乘法一定最优。

## 十二、实验设计

可信度识别指标：

- Precision
- Recall
- F1
- ROC-AUC / PR-AUC（在类别不平衡时优先 PR-AUC）
- 误报率
- 漏报率
- 不同异常类型的召回率

质量预测指标：

- MAE
- RMSE
- R²
- 多步预测误差
- 不同工况下的分组误差

预警指标：

- Precision / Recall / F1
- 平均提前预警时间
- 漏报率
- 单位时间误报次数
- 越界时间误差

鲁棒性实验：

- 污染比例：0%、5%、10%、20%、30%
- 噪声强度
- 持续偏移幅度
- 漂移速度
- 缺失比例
- 训练与测试工况分布变化
- 小样本条件

消融实验：

- 去掉可信度；
- 去掉数据修正；
- 去掉物理损失；
- 固定/动态物理参数；
- 去掉参数平滑；
- 去掉风险累积；
- 去掉区块链完整性；
- 固定权重与动态权重比较。

所有主要实验至少运行 5 个随机种子，报告均值和标准差。

## 十三、工程要求

技术栈优先：

- Python 3.11
- PyTorch
- NumPy
- pandas
- scikit-learn
- SciPy
- matplotlib
- PyYAML
- pytest
- Optuna 仅在后续超参数搜索阶段使用

工程规则：

1. 不把全部逻辑堆进 Notebook；
2. 配置、数据生成、预处理、模型、训练、评估、绘图分离；
3. 所有实验可通过命令行和配置文件复现；
4. 固定随机种子；
5. 自动保存配置快照、Git commit、指标、图表和模型；
6. 测试集在最终评估前不得用于调参；
7. 每个重要函数有类型标注、docstring 和异常处理；
8. 重要数学实现需要单元测试；
9. 不随意新增大型依赖；
10. 不删除用户原始文件或覆盖实验结果。

## 十四、工作方式

这是一个长周期项目。开始任何复杂实现前：

1. 先检查仓库现状；
2. 阅读全部说明；
3. 新建或更新 `docs/plans/current_execplan.md`；
4. 列出已知事实、假设、未知项、风险、验收标准；
5. 给出最小可行实现；
6. 再开始编码；
7. 每完成一个里程碑，更新 `docs/STATUS.md`、`docs/DECISIONS.md` 和执行计划；
8. 运行测试和最小实验；
9. 审查差异；
10. 提交清晰的阶段总结。

不要把每个小问题都抛回给用户。只有当缺少的信息会实质改变科学结论或不可逆实现时才提问。其他情况下采用最保守、可配置、可回退的假设，并写入决策日志。

## 十五、当前立即执行的任务

请执行 **Milestone 0：仓库体检与第一阶段实施计划**。

具体要求：

1. 检查仓库文件和现有代码，不要假设仓库为空；
2. 读取所有项目文档和原始材料；
3. 总结项目目标、当前状态、已有资产和明显冲突；
4. 创建 `docs/plans/current_execplan.md`，规划：
   - 仿真数据生成；
   - 异常注入；
   - 可信度计算；
   - PCA 可视化；
   - 异常识别评估；
   - LSTM 基线；
   - 可信度修正 LSTM；
5. 若代码尚未存在，创建最小目录和空模块，但不要一次性实现全部高级模型；
6. 创建 `configs/baseline.yaml`；
7. 创建或更新 `requirements.txt` / `pyproject.toml`；
8. 创建最小测试框架；
9. 在完成规划后，直接实现第一小步：
   - 可复现的正常时序仿真器；
   - 至少 spike、bias、drift 三种异常注入；
   - 异常掩码；
   - 基本数据校验测试；
   - 一张示例曲线；
10. 最后给出：
   - 修改文件清单；
   - 运行命令；
   - 测试结果；
   - 当前限制；
   - 下一里程碑；
   - 需要用户确认的事项（仅列真正关键的）。

## 十六、完成定义

本轮任务只有同时满足以下条件才算完成：

- 代码可以从干净环境运行；
- 仿真结果固定随机种子后可重复；
- 异常标签与注入位置一致；
- 数据范围、缺失、形状和质量单调性/合理性有测试；
- 示例图自动保存到 `results/figures/`；
- 不存在训练测试泄漏；
- 文档已更新；
- 没有伪造尚未完成的实验结果；
- 下一步工作已经写入执行计划。

现在开始：先阅读仓库，再写执行计划，再实现 Milestone 0 的第一小步。
