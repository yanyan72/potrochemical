请先阅读仓库中的 `MASTER_PROMPT_FOR_CODEX_WORK.md` 和 `AGENTS.md`。检查当前仓库，不要假设为空。

本轮只完成 Milestone 0：

1. 创建或更新 `docs/plans/current_execplan.md`；
2. 建立最小 Python 工程；
3. 实现可复现的正常时序仿真器；
4. 实现 spike、bias、drift 三种异常；
5. 返回异常掩码和异常元数据；
6. 添加数据范围、形状、复现性、掩码一致性测试；
7. 生成一张示例图；
8. 更新 `docs/STATUS.md` 和 `docs/SESSION_HANDOFF.md`。

不要实现 LSTM、PINN、区块链或界面。完成后运行测试并给出准确路径。
